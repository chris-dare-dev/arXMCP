- The flat, cross-repo `frontend-uplift-source-registry.md` §2 / `frontend-uplift-motion-vocabulary.md` §10 candidate tables are React/npm-project-oriented (Tailwind, Radix, shadcn, Framer Motion, anime.js, TanStack, etc.) and do NOT apply as-is to a no-build-chain repo like arXMCP — every entry in them is an automatic Phase-3 BLOCKER here. Read the repo-local overlay (`arxmcp-design-system.md` §9 house thesis + architectural locks) as the actual authority and say so explicitly in the brief's parking-lot section, so the synthesizer doesn't independently reach for the flat canon's defaults.
- For htmx-extensions candidates: the `bigskysoftware/htmx-extensions` GitHub repo has NO root `LICENSE`, no `package.json` license field, and `null` on the npm registry — but EACH per-extension subdirectory (`src/<ext>/LICENSE`) carries its own 0BSD grant. Check the per-extension path before flagging a license gap; the repo-root/npm-registry check alone gives a false negative.
- Live-verify Baseline status precisely (MDN + the actual 30-month Widely-Available clock), not marketing-blog "Baseline 2026!" framing — several 2026 blog posts call a feature "Baseline" the moment cross-browser support completes (Newly Available), which can be a year+ before the Widely-Available line. Cite both dates when they differ (popover, anchor positioning, field-sizing, light-dark(), text-wrap: balance were all still short of Widely-Available as of Aug 2026 despite being called "Baseline 2026" in secondary sources).
- Ground every candidate in an exact file:line citation from a live read of the repo's actual CSS/templates (not the design-system doc's prose alone, which can drift) — e.g. `arxmcp-design-system.md` said "frontend/static/app.css" but the real path is "server/frontend/static/app.css"; always verify against `ls`/`Read` before citing.
