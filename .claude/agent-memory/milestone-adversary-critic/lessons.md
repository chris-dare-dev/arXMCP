2026-07-05 | paper-metadata-m1 | Operator-action acceptance legs slip silently — check var/ on disk for the milestone's namesake artifact (no paper_metadata.db existed despite "AC1 met offline"), and on this Windows checkout verify %G? signatures: commits are unsigned per a claimed orchestrator dispatch waiver that CLAUDE.md §4.3 does not yet record.
2026-07-12 | data-plane-governance-m1 | Constitution/ADR diffs: compare the CLAUDE.md short form against the ADR clause-by-clause (compression dropped the "shipped distribution" scope qualifier) and grep-verify every attributed quote (ADR:91 quote lives in R0:16, not the briefs README); also: %G?=G again (signing waiver did not recur), and worktree CLAUDE.md line numbers run +11 vs committed (Obsidian frontmatter) — cite worktree numbers with verbatim anchors.
- 2026-07-12 (data-plane-governance-m3): Docs-only policy diffs still demand fact-checking every file:line cite against the repo — verified lean_verify.py:290-298, get_definitions format-not-existence gap, R0 11 axes, and the wont-list enforcement-deferral (all accurate). Best catch was an AC-placement asymmetry: R3 put the policy by-path ref in its ## Gates section but R5 put it only in Key results, so a strict "gates reference it" reading (MEDIUM) is unmet for R5. Independently re-grep "exactly N absence claims" exhaustiveness claims with a BROADER pattern than the doc used — R5:78 "nothing serves" and compound R6:15 lines are design/prior-art, correctly out of scope. Sibling-repo facts (rigor.py, BridgelandStability Spec.lean) are unverifiable from this repo; flag as caveat, not finding, absent counter-evidence.
2026-07-12 | adhoc-20260712-955c958 | Surgical kuzu del-db->close fix was clean; best (only non-LOW) catch was doc drift — the committed CLAUDE.md test-status block still listed the 8 tests as win32-GUARDED and the close-discipline as an "Open follow-up" the diff had just completed; cite worktree CLAUDE.md line (92, +11 from Obsidian frontmatter) with a verbatim anchor, not the committed line. %G?=G held again.
2026-07-12 | adhoc-20260712-698fead | Residual kuzu del-db->close fix (3 sites) was correct per-structure (one-guard sites 1/2 where Database precedes try, two-guard site 3 where it is inside try); best catch was the honestly-disclosed non-deterministic regression test that passes on reverted del db code (propose a kuzu.Database spy asserting .close() called). %G?=G held.
- 2026-07-12 (source-truth-m1): When a new arXiv-facing fetcher "re-mirrors" `ingest/oai_delta._fetch_page`, it inherits oai_delta's UNCLAMPED `Retry-After` parse — `Retry-After: 0` -> `sleep(0)` busy-loop, defeating the exponential backoff (the value is `not None`, so `backoff` is never consulted). The sibling `tools/arxiv_fetch.parse_retry_after` clamps to a floor and documents the live-observed `Retry-After: 0` hazard; check WHICH retry-after helper a new fetcher copied. Also: a NULL provenance field (parse-artifact sha) with no companion `_status` marker is silent-not-abstention — grep each nullable checksum for a paired status column.
- 2026-07-13 (source-truth-m2): For LanceDB add_columns→hydrate CLIs, verify the migrate-THEN-hydrate composition on the SAME handle is TESTED, not just the two halves separately; tests that build tables at the final schema skip the go-live path, and an in-memory abstention report can falsely print "resolved" if a stale-handle merge_insert silently no-ops the new columns (no post-write read-back). Reproduced the mechanism is fine on lancedb 0.30.2. Also: float32 embeddings round-trip byte-identical through to_pylist/from_pylist(schema=table.schema), so that write path is safe to steelman-clear.
- 2026-07-13 (source-truth-m5): "explicit null not omission" ACs must be tested at the WIRE, not just the handler dict — FastMCP convert_result (func_metadata.py:130) preserves None only because it calls model_dump(mode="json") WITHOUT exclude_none; drive app.state.mcp_server.call_tool() to confirm (returns (unstructured, structured) tuple; all null keys survived here) and propose a _call_tool-harness guard since a dep bump could flip the default. Best real catch was field-name overload: surfacing a pre-named m2 column (truncated = ingest/STMT_MAX_TOKENS provenance) next to serving-time truncated_for_license/body_truncated collides and can disagree bidirectionally (§4.9 evidence-misread) — MEDIUM doc/comment, not a bug. row.get() landmine + license_ref-advisory were genuinely clean (grep server/ for the advisory field: only in the dict + changelog). %G?=G held.
- 2026-07-13 (source-truth-m3): A "read-only, generated on-read" resource can still WRITE via a store's open()/migration side effect — OperatorSettingsStore.open -> _apply_migrations runs CREATE TABLE IF NOT EXISTS + sentinel INSERT, so a resources/read of the manifest turns an absent operator_settings table present in notebooks.db (PROBE IT: snapshot sqlite_master before/after build_manifest on a fresh notebooks.db — the module's is_file() guard stops file-creation but NOT table-creation). The "no writes anywhere in this path" docstring/brief claim was false and the read-purity test only covered the absent-FILE case. Grep server/main.py to confirm the lifespan never pre-opens the settings store (only comment refs) => reachable in prod. Contrast DocumentsStore.open (version-gated, no unconditional DDL) vs OperatorSettingsStore.open (unconditional CREATE IF NOT EXISTS) — same "open a store to read" idiom, different write footprint. Core content-hash/AC2/boundary were solid: sort_keys handles dict order but arrays are explicitly sorted, AC2 has synthetic withdrawn+superseded (not vacuous), boundary pins EXPECTED_TOOL_SCHEMA_SHA256 net-new. %G?=G held. — FastMCP convert_result (func_metadata.py:130) preserves None only because it calls model_dump(mode="json") WITHOUT exclude_none; drive app.state.mcp_server.call_tool() to confirm (returns (unstructured, structured) tuple; all null keys survived here) and propose a _call_tool-harness guard since a dep bump could flip the default. Best real catch was field-name overload: surfacing a pre-named m2 column (truncated = ingest/STMT_MAX_TOKENS provenance) next to serving-time truncated_for_license/body_truncated collides and can disagree bidirectionally (§4.9 evidence-misread) — MEDIUM doc/comment, not a bug. row.get() landmine + license_ref-advisory were genuinely clean (grep server/ for the advisory field: only in the dict + changelog). %G?=G held.
[CONFIRMED] 2026-08-03 | verification-contract-m1 | For a wire-value RENAME milestone the code half is usually complete (grep every comparison site + the schema enum + the negative `!=` assertions for preserved polarity — all clean here); the real findings live in the docs that assert the OLD value in the present tense: trust-language-policy.md:33-38's dated amendment (b) literally said `status` "still carries the value 'ok', deliberately" and CLAUDE.md:412-413 named it as the live token, neither amended — both CRITICAL per the doc-drift analog, and both have a precedented cheap fix (append a dated `>` amendment, the pattern the policy file itself already uses). Also: a NEW doc authored in the same commit can be stale at birth — the ADR cited `lean_verify.py:1077-1148`, exactly the PRE-diff range, because the same commit inserted 10 lines above it; always re-resolve a diff-authored doc's own file:line cites against the POST-diff tree. Diff was 345+55 = 400 LOC exactly, which does NOT trip the >400 auto-finding — state the arithmetic so the non-filing is auditable. %G?=G held; unpushed; no roadmap.yaml touch.
2026-08-04 | ui-uplift-m9 | For a "derived policy test" milestone, DON'T review the extractor by reading it — import it and run synthetic probes: this one's AC3 forward-binding claim died on two shapes (single-quoted `class='...'` since the regex is `"`-only, and any literal under an f-string interpolation since visit_JoinedStr omits generic_visit), while `.format()` and adjacent-literal folding were genuinely fine. Two more reusable moves: (a) check an exemption list in BOTH directions — this one asserted "still unstyled" but never "still emitted", and the implement synthesis openly said that half was cut while the class docstring still claimed "cannot go stale in either direction" (implement/synthesis.md is the cheapest source of self-admitted gaps); (b) fact-check the rationale comments, not just the code — "app.css has no bare `.foo {}` rules" was repeated in the docstring, a helper docstring AND the commit message and is false at app.css:50/53/147/174/190-193/201. Also grep for a SECOND producer of an allow-listed dynamic family (`_PARSE_STATUS_CSS` at ui.py:114 feeds the same `status-badge--` prefix via a template, unpinned). 520-LOC test-only diff => mandatory HIGH auto-finding stands; the sibling-file-size justification is weak (file size != single-commit diff) and a `tests/_ui_class_scan.py` helper split mirroring `tests/_graph_helpers.py` was available. %G?=G held; unpushed; no roadmap.yaml touch. Worktree has no .venv — use the main repo's ../../../.venv/Scripts/python.exe.
[CONFIRMED] 2026-08-04 | ui-uplift-m6 | For a colour/contrast milestone, re-derive the maths by an INDEPENDENT path before critiquing it (OKLab->LMS->XYZ D65->linear sRGB vs Bottosson's direct matrix agreed byte-for-byte on all 14 tokens; 0.03928 vs 0.04045 provably moves no 8-bit ratio, so both are non-findings) — the real defects live in the hand-maintained PAIRS *inventory*, not the generated arithmetic: read the roadmap.yaml AC verbatim and count its enumerated items (AC#3 named FIVE --accent roles; `test_accent_satisfies_all_five_roles` covered four — "badge-flash tint" was missing, and the alpha composite it names fails 5 of 8 pill pairs). Enumerate composited/transient states the sweep can't see by grepping app.css for `opacity:`, `color-mix(`, and `@keyframes` backgrounds; and check every literal ground a token is drawn against (light --border was solved vs --bg 3.30:1 but its real binding grounds are `th #f0f0f0` 3.073 and `tr:hover` 3.040). Also: hand-typed prose OUTSIDE a generated-marker region is unguarded by the staleness test (headline said "34 light, 34 dark", real split 36/32), and a `hex in svg.lower()` sync assertion is satisfied by the hex sitting in the file's own XML comment. Diff 1560 LOC — auto-finding deliberately NOT filed per an orchestrator-recorded user `--allow-large-diff`; say so in the exec summary so the omission is auditable. %G?=G held on both commits.
2026-08-04 | adhoc-20260804-c8e6048 | For a fail-safe REGEX fix, re-read the research brief's own characterisation of the defect and check the implementation keys on IT: brief-2:155-158 said the bug "is not really about `in` … it's about same-line co-occurrence of an unrecognized token and a keyword", but the fix keyed on `in`, so `/-- Helper. -/ axiom evil : False` + a real theorem still returns `(['harmless'], True)` -> `clean` (HIGH; `deriving instance … for Foo` too). Probe by IMPORTING and running `_declaration_names`, never by reading the regex. Second reusable move: a `.match()`-anchored regex containing `.*?` is a substring scan wearing an anchor — the rationale comment claimed ".match() … the repo-wide invariant that keeps comments safe" and comments/docstrings/tactic lines with `in <keyword>` now all count as sites, the exact hazard brief-2:186-190 pre-flagged; the AC's own "comment control" test used comments with NO `in`, so it passes identically with and without the fix (vacuous w.r.t. the new code — always check whether a control test can distinguish pre/post). Best CRITICAL was again doc drift in the present tense: CLAUDE.md 4.10 rule 3 (:475-500) carried a DATED four-row measurement table, two rows of which the diff falsified, plus three file:line cites the +37-line insertion moved (:479/:1123/:613 -> :506/:1158/:646) — re-resolve every cite a diff shifts. Dispatch metadata had a mistyped head sha (…2fd7c0f5 vs real …2fd7c750); `git cat-file -t` it before trusting the range. Diff 187 LOC so no auto-finding — stated the arithmetic. Full suite re-run independently: exactly the 8 claimed env-bound failures, no more. %G?=G held; no roadmap.yaml touch.
[CONFIRMED] 2026-08-04 | ui-uplift-m7 | For a TYPE-SCALE milestone the payload finding is a size-cascade interaction no token test can see: a bare `code { font-size: var(--text-small) }` rule fires on `<code>` nested inside a heading, and `notebook_detail.html:9` is `<h2><code>{{slug}}</code></h2>` — 17.6px -> 13px, i.e. the h2 renders SMALLER than body and AC#1's step inverts to 0.81x (pre-m7 it inherited full size because `.card h2 {font-size:1.1rem}` is a length, which resets Blink's keyword-derived monospace-size quirk). Always grep templates for `<h[1-3]...><code>` and for an unstyled `<small>` nested inside a shrunk parent (`.status-badge` 12->11px put `status-badge__remediation` at ~9.2px via UA `smaller`). Verify the AC#2 inventory by TICKING OFF every numbered row of brief-1 §2 against the post-diff tree — 32/33 landed and the one miss (site 10, `<span class="hint">(ingest {{latest_run.status}})</span>`) is absent from the implement synthesis's own list of closed sites, so diff the synthesis's enumeration against the brief's. Also: `wc -l` the capped file rather than believing the comment — the checked-in cap comment said "471 to ~400" while the file is 478/480 and the synthesis said 471->478. Check state.json for `allow_large_diff` BEFORE filing the >400 auto-finding (it was true here, as at m6 — say so in the exec summary). Baseline re-run gave exactly 8 env-bound failures incl. the network-flaky `test_cite_neighbors_wired`, confirming the dispatch over the implementer's 7. %G?=G on all three commits; no roadmap.yaml touch. Beware `pytest ... | tail -N` — it truncates the counts line AND reports the pipe's exit status.
2026-08-04 | ui-uplift-m10 | For a NEW-COLOUR-TOKEN milestone, re-derive the token independently (Bottosson OKLCH matrices, round to 8-bit) AND enumerate the token's real grounds from the TEMPLATE tree, not the comment: here all 8 registry rows and the 5.448:1 tightest-pill claim were exact, and probing the deliberately-unregistered `--bg` grounds (6.82/7.70) confirmed nothing was hidden by the omission — so the payload findings were elsewhere. Three reusable moves: (a) when a comment justifies a target by "the band the greys it replaces already occupied ... so nothing gets lighter", MEASURE all of them — the eleven legacy greys spanned 4.886-11.467, and #444/#c9d1d9/#b3b9c0 (and even the cited #555 at 7.249) all get LIGHTER at 7.00, a false inference repeated in tokens.css, the synthesis and the artifact; (b) when a debt-deferral dict is emptied as the epic's finish line, check whether ANYTHING asserts the emptiness — both self-cleaning tests here comprehend over the dict and pass vacuously on `{}` (verified by forcing an entry: zero offenders, zero failures), and with §4.1's no-PR/no-CI posture the "unconditional binding" lasts exactly one line-edit; the fix is the repo's own three-cap-test "deliberate raise" pattern; (c) `list-style: none` on the product's FIRST/only `<ul>` with no `role="list"` drops list semantics in WebKit — grep `list-style` + `<ul` + `role="list"` together on any list-styling milestone. Re-ran the extractor with synthetic probes rather than reading it (m9 lesson held): comment-only classes correctly DON'T satisfy `_css_defines_class`, and m9's single-quote hole is FIXED in the base (`_CLASS_ATTR_RE` now `"..."|'...'`) — that half of the m9 lesson is stale. Doc-cite drift again: implement/synthesis.md cited app.css:224-228 for a rule at 282-286 and :281-298 for one at 316-322 (verification-contract-m1's "stale at birth" lesson, CONFIRMED), and its frontmatter shas (0edc12a/263bdba) are the pre-rebase worktree ones, not the landed b742b59/55545d7 — `git merge-base --is-ancestor` every sha a synthesis names. Cap history in the COMMENT blocks was accurate (m6 400->480 8ee611e, m7-rect 480->520 f4b6bb1, m10 520->600) but the assert MESSAGE says "600-line cap (revised in ui-uplift-m6 from 400)" in 2 of 3 files — check the message string separately from the history block. allow_large_diff=true again (m6/m7 precedent) so no >400 auto-finding; stated the 1974/274 split. Full suite re-run: exactly the 8 env-bound failures incl. the flaky test_cite_neighbors_wired. %G?=G x4; unpushed (origin/main still at 216aff0); no roadmap.yaml touch.
2026-08-04 | ui-uplift-m8 | For a milestone that RETIRES registry rows, treat every retirement rationale as a claim to falsify: this one dropped both `--border on tbody tr:hover` pairs saying "no FULL-WEIGHT rule is drawn against the row-hover ground any more", but `thead th { border-block-end: var(--rule-section) }` still abuts the FIRST tbody row, so the pair renders at 3.0401 light / 3.0804 dark — tighter than the artifact's own generated "Tightest gated pair 3.129:1" headline (my independent alpha-composite reproduced that 3.129 exactly, which is what made the counter-measurement trustworthy: always re-derive the number the artifact BOASTS about, not only the ones under scrutiny). Enumerate a token's consumers by grepping `var(--token)` rather than trusting the comment — `--fg-muted` has FOUR consumers and tokens.css/registry/artifact all say three (.discover-abstract missing). Check a re-grounded control row against the LIGHT rule too: light input/textarea keep `background:#fff` (only the dark block overrides), so "th / input / textarea text on --card-bg" over-registers in light AND dropped the real `--fg on #fff` pair — the milestone's own Deferred list said so. When a milestone edits a stale prose region and calls it audited, re-check the WHOLE sentence: the artifact's m7-sizes paragraph still put .discover-abstract at 13px (it is --text-meta 11px) and .discover-title at inherited 16px (it is --text-section 20px). Cap comments drift the same way — two of three lockstep blocks still read "lands at 575 of 600, a 25-line margin" at 599/600. The D1 exemption itself HELD at all 7 sites (every boundary nothing else carries takes --rule-section) — the payload was one registry row over. Measure the whitespace when an exemption cites it: dl.meta inter-row 14.5px < intra-dd 19.5px, i.e. inverted. allow_large_diff=true again (m6/m7/m10 precedent) so no >400 auto-finding; stated 1385 changed / 1191 authored / 194 generated. Baseline re-run: exactly the 8 env-bound failures. %G? = G x5; no roadmap.yaml touch; push declared not performed.
2026-08-05 | ui-uplift-m12 | For a REORDER milestone the payload is the swap the reorder invalidated that lives OUTSIDE the template: m12 added `show:#papers-tbody:bottom` to the two template `beforeend` forms and missed the third, `_discover_results_fragment`'s per-candidate Add button (server/routes/notebooks.py:752), which appends into the same tbody from inside the disclosure — grep the whole of server/ for the target id, never trust a synthesis sentence of the form "the two X are the whole of it". Three reusable moves: (a) when a guard is re-transcribed rather than re-sorted, check the guard's VOCABULARY separately from its data — m8's TestSectioningElementDecision got a correct per-site re-decision and an honest unchanged EXPECTED=(2,7), but its docstring, the :370 comment and three failure messages still say "top-level blocks" about 7 blocks of which 5 are now nested, and the ordering is newly pinned to the five DIVs staying at column 0 with only prose defending that premise (the structural property IS measured, by TestManageDisclosureNesting — so this is NOT the m8 keep-passing class; say so explicitly when the dispatch asks); (b) a "recorded residual" is a claim to check for DIRECTION — the synthesis disclosed stale-`running`-over-settled but not the harmful inverse, summary reading `success`/`none` while an in-page-started run polls underneath, which is exactly the invisibility AC#2 exists to prevent; (c) an AC with a measurable clause (`visible without scrolling`, epic's y=1823 of 2343) guarded only by `DETAIL.index(a) < DETAIL.index(b)` is order-proven and visibility-unverified — no browser harness exists here so a documented derivation is the available form. Verified the 62-line relocation claim EXACTLY by multiset-comparing added vs deleted diff lines (142+489+93+124=848) — scope accounting was honest. Cap comments wrong again (635 in three lockstep files, app.css is 627, synthesis had it right) but histories were genuinely APPENDED, not blanket-replaced. Discovery cites: read .claude/ paths with Read (rg cannot see them) and verify each — art-direction:372/:434, challenge:98-104, final-report:393-394 all EXACT; challenge.md:107 wrong (cue string is at 115-116) and inherited from research/brief-2.md:106. AC#2's predicate is provably complete: store vocabulary is only running/success/failed (notebooks_store.py:572-574) + route-synthesised `none`, and unknown values fall OPEN. allow_large_diff=true again (m6/m7/m8/m10) so no >400 auto-finding — stated the arithmetic. Independent full-suite re-run: exactly the 8 env-bound failures incl. test_cite_neighbors_wired (local var/ state, not a flake; the implementer's worktree 7 is the same fact). %G?=G x3; unpushed (origin/main at 54f3cd3); no roadmap.yaml touch.
[CONFIRMED] 2026-08-06 | desktop-distribution-spike-1 | Bundle evidence must use lstat-based censuses, content-aware manifests, and whole-tree build-root scans because symlinks and dist-info metadata defeat naive size, immutability, and host-path claims.
2026-08-06 | desktop-distribution-m2 | Relocation evidence must snapshot the installed environment's parent, read back shared-database sentinels, and hash regular-file contents because venv-only metadata manifests miss sibling writes and equal-size rewrites.
2026-08-07 | desktop-distribution-m3 | In managed sandboxes, `%G? = N` can reflect denied GnuPG trustdb access even when the commit contains a `gpgsig`; inspect raw commit headers and rely on an unsandboxed `git verify-commit` before filing an unsigned-commit finding.
2026-08-07 | desktop-distribution-m5 | For a "hand-drive uvicorn" milestone the payload is a docstring CLAIM about a framework default: `_serve` argued `timeout_graceful_shutdown=None` is "a superset of the contract's floor", but uvicorn's `Server.shutdown()` with no timer spins `while (connections or tasks) and not force_exit` and `mcp/server/streamable_http.py:659` opens a real SSE stream on `GET /mcp/` EVEN under `json_response=True` (read the installed SDK, don't infer from the mount flag) — so a connected MCP client makes every quit a 40 s SIGKILL that skips the lifespan's LanceDB/Kùzu closes. Four reusable moves: (a) when a milestone gates ONE path, grep create_app for the documented SUPERSET of that path — `/status` (health.py:617, "A SUPERSET of /readyz") and `/ui/status-badge` are ungated on the same port, and `SecFetchSiteMiddleware` passes a header-less client (middleware.py:622), so the gate is a handshake not a confidentiality control; (b) read the actual vendored dep before filing a platform finding — `~/.cargo/registry/.../tauri-plugin-single-instance-2.4.3/platform_impl/macos.rs:61` derives `/tmp/{identifier with . and - -> _}_si.sock`, so the hard-coded literal was RIGHT (no finding) while the `#[cfg(unix)]` gate was WRONG because linux.rs uses zbus/DBus (real finding); (c) diff the docs the new code CITES — `process_control.rs:5` cites apps/desktop/README.md, whose :5/:103 still said the Tauri shell and lifecycle adapter "do not yet" exist, and CLAUDE.md:258 still said "Nine test markers exist" against ten in pyproject; (d) steelman kills more than it files — Python `socket.close()` is idempotent so the uvicorn/asyncio/finally triple-close is a non-finding, Rust `read_frame`'s `read_until` desyncs on overflow EXACTLY like Python's `readline`, so the "mirrors byte-for-byte" comment is accurate, and the three `?` early-returns after `command.spawn()` all drop stdin => EOF => the child self-exits, so the apparent orphan is not one. allow_large_diff=true in state.json:36 so no >400 auto-finding; stated 2109/5 ex-lock and 7059/109 with it. %G?=G x4 with Co-Authored-By on all four; no roadmap.yaml touch. `findings.py extract` needs `--check` (or `--id`) or it refuses to lint.
