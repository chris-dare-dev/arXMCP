# capability-scout-adversary — lessons

<!-- Append one generalizable lesson per line as scout runs surface them.
     Format: `YYYY-MM-DD [<scout-id>] <lesson>`. Append only; do not delete. -->
2026-05-28 [2026q2-notebook-ux-storage-ops] Design-constitution notes (especially numbered notes + CLAUDE.md §5 layout) are never retroactively updated when a post-E14 feature stream (notebook UI, textbook-ingest) ships new top-level modules; security audits (E13_S06) scoped to the prior snapshot miss the new surface entirely — always cross-check note freshness before treating "no frontend exists" claims as current.
