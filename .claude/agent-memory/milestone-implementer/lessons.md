2026-08-04 | ui-uplift-m6 | Never pipe a check gate through `tail`/`grep`: the shell reports the PIPE's exit code, not pytest's, so a red suite reads as green — redirect to a file and echo `$?` separately.
2026-08-04 | ui-uplift-m6 | A worktree has no `.venv` (gitignored, not shared) and no untracked files from the main tree; run gates with the MAIN tree's `./.venv/Scripts/python.exe` using the worktree as cwd, and read research briefs from the main tree's `.claude/notes/`.
2026-08-04 | ui-uplift-m6 | When a milestone moves a token/constant, grep the whole repo for byte-copies of its OLD value before finishing: pinned tests, sibling literals (`th{background:#161b22}`), non-CSS assets (favicon.svg) and prose comments all drift silently, and only the pinned tests fail loudly.
2026-07-05 | paper-metadata-m1 | Structural source-scan tests must exclude module docstrings (grep for import paths/symbols, not prose) — a docstring explaining WHY a table is avoided legitimately names it; also: on this Windows checkout, tests/test_arxiv_fetch.py UserAgent tests fail from the workstation's persisted operator_settings email and test_operator_settings.py fails on path separators — pre-existing, not regressions.
2026-08-04 | ui-uplift-m7 | [CONFIRMED] The m6 "grep for byte-copies before finishing" lesson generalises from VALUES to FILE PATHS: moving app.css's `:root` blocks into tokens.css broke six test modules that parsed `:root` out of app.css, and the two non-test consumers (`tools/wheel_install_check.py`'s required-file list, `test_wheel_packaging.py`'s named casualties) would have shipped a wheel whose console renders every var() as its initial value — silently, because a source checkout has the file on disk either way.
2026-08-04 | ui-uplift-m7 | [CONFIRMED] A worktree cannot `git checkout main` when the shared checkout already has main out (git refuses: "already used by worktree"), and a worktree-isolated agent cannot run git against the parent either — so the "land commits on main" branch policy is mechanically unavailable from a worktree and the commit lands on the worktree branch for the orchestrator to fast-forward. Say so rather than reporting a policy-compliant branch.
2026-08-04 | ui-uplift-m8 | [CONFIRMED] The m6 worktree lesson has a third clause: measure the BASELINE FAILURE SET yourself in the worktree instead of trusting the dispatch brief's count — a worktree has no `var/` tree at all, so `test_cite_neighbors_wired` PASSES there (graph_status correctly reports `absent`) and the brief's "8 pre-existing failures" is 7. Reporting 8 would have hidden a real new failure inside the allowance.
2026-08-04 | ui-uplift-m8 | A token whose value is a whole `border-*` SHORTHAND (`1px solid var(--border)`, `1px dotted color-mix(...)`) needs only ONE declaration for both colour schemes, because custom properties substitute at use time against whichever `:root` won — three declarations instead of six, and it is the honest reason to add its prefix to `NON_COLOUR_TOKEN_PREFIXES` rather than a dodge around the oklch guard.
2026-08-04 | ui-uplift-m8 | When a milestone's own design retires the argument a research brief gave for a decision, say so AT the decision instead of repeating the brief: keeping `tbody tr:hover` on `--card-bg` was justified by a 2.9533:1 SC 1.4.11 cliff that stopped existing the moment the row rule dropped to the tinted rung, so the stylesheet records the role argument and marks the cliff RETIRED — a comment that restates a dead reason is worse than no comment.
2026-08-04 | ui-uplift-m8 | A line cap on a stylesheet caps DOCUMENTATION, not CSS (app.css is 60% comments), so the way to fit a milestone that must record five decisions is to put each argument in exactly ONE file and point at it from the others — the ladder's grading/exemption/rejected-alternative prose in tokens.css let app.css land at 599/600 with no fourth raise. Do that BEFORE trimming, not after nibbling at other milestones' rationale.
2026-08-04 | ui-uplift-m8 | Deleting a CSS primitive has no partial-but-coherent commit: templates, both stylesheets, four named guards, the BAN-R2 coverage gate and the contrast registry all fail together, so the scope-abort protocol cannot be followed literally — finish, then report the overrun with the authored-vs-generated split stated (here 1191 authored / 194 generated, i.e. the artifact does NOT excuse it).
2026-08-04 | ui-uplift-m7 | `re.findall(r"prop:\s*(?!var\()([^;]+);")` does NOT exclude `prop: var(--x);` — `\s*` backtracks to zero width, the lookahead passes at the space, and every declaration matches. Capture the value and filter in Python instead of encoding the negation in the regex.
2026-08-04 | ui-uplift-m10 | Minting a colour token is never a one-file change: `--fg-muted` pulled in the OKLCH-shape test, EIGHT new pair-registry rows (2 card grounds + 6 status-pill grounds), the regenerated contrast artifact, AND two hand-written prose regions of that artifact sitting OUTSIDE the generated markers (the token-family table and the per-surface size enumeration) — budget the token's blast radius before deciding to mint, not after.
2026-08-04 | ui-uplift-m10 | Register a pair only where it ACTUALLY renders: `.status-badge__remediation` gets no `ok`-pill row because `_build_remediation_block` returns "" for ok, and `--fg-muted` gets no `--bg` row because every consumer sits inside `.card`. A registered-but-unrendered pair is the same class of error as an unregistered rendered one, and it is the easier one to add by reflex.
2026-08-04 | ui-uplift-m10 | A `git diff --stat` line overstates authored work whenever a generated artifact is in the diff: 168 of this milestone's 466 churned lines were the contrast table renumbering itself after 8 rows were inserted near the top. Report the split, not the raw total.
2026-08-05 | ui-uplift-m12 | [CONFIRMED] The m8 worktree-baseline lesson held again: the dispatch said 8 pre-existing failures, the worktree showed 7 — `test_cite_neighbors_wired` passes here because a worktree has no `var/` — so measure the baseline yourself or a real new failure hides inside the inherited allowance.
2026-08-05 | ui-uplift-m12 | The m10 "report the split" lesson generalises past generated artifacts to BLOCK MOVES: `git diff` counts a relocated section twice, so 124 of this milestone's 848 lines were the papers `<section>` appearing verbatim on both sides. Compute it (`Counter(added) & Counter(deleted)`), don't estimate it.
2026-08-05 | ui-uplift-m12 | Re-indenting blocks you nest inside a new wrapper is not free twice over: it costs ~380 lines of pure whitespace churn AND it can silently shrink a structural guard's view — `TestSectioningElementDecision` matches `^<(section|div)>` at column 0, so indenting five DIVs into a `<details>` would have collapsed a seven-entry ordered per-site record to two. Leave them at column 0 and pin the nesting with its own assertion.
2026-08-05 | ui-uplift-m12 | A CSS rule ladder built on a DIRECT-CHILD combinator (`main > … + …`) breaks silently the moment a milestone nests blocks one level deeper, and token/horizontality guards do not catch it because they never assert COVERAGE. When you nest, ship a reachability guard in the same commit — and expect the sibling exemption registry (m8's `TINTED_SITES`) to demand the new nested site with its second cue.
2026-08-06 | desktop-distribution-spike-2 | A managed macOS gate can deny loopback `bind()` with EPERM; rerun the exact gate via approved unsandboxed execution before classifying socket tests as regressions.
2026-08-06 | desktop-distribution-m1 | Scope guards must add untracked-file line counts to `git diff --stat`, which omits them until staging and can hide a threshold breach.
2026-08-06 | desktop-distribution-m1 | Installed-Config tests in a source checkout must disable source-layout detection and relocate autouse path aliases below the test root, or the fixtures mask strict installed-mode confinement.
2026-08-06 | desktop-distribution-spike-1 | A frozen macOS native probe must run real FAISS search plus Torch forward: import-only checks miss duplicate-libomp behavior, while PyInstaller RPATH rewriting can change the outcome.
2026-08-06 | desktop-distribution-m2 | A relocation smoke must scrub ambient ARXMCP/Python/loader variables and watch the checkout, venv, arbitrary CWD, and enclosing sandbox; a temporary data root plus `/healthz` alone proves neither import isolation nor write confinement.
2026-08-07 | desktop-distribution-spike-3 | Tauri shell `sidecar()` resolves a literal filename beside the host binary; compile the target triple into that name and stage the matching `<name>-<triple>` executable there before testing stdin leases and termination events.
2026-08-07 | desktop-distribution-m3 | `cargo test` builds a binary test harness under `target/debug/deps`, not the runnable `target/debug/<bin>`; run a locked `cargo build --bin <name>` before a cross-language process gate and pass its absolute path explicitly.
