2026-07-12 | data-plane-governance-m1 | CLAUDE.md has an obsidian-strip clean filter (worktree +22 lines vs committed blob), so anchor on text never line arithmetic; on a pre-dirty tree, splitting git diff by a content marker unique to the foreign hunk (metadata_status=) and git apply --cached the rest gave clean hunk-scoped staging on the first pass.
2026-07-12 | data-plane-governance-m3 | Cross-repo file:line cites drift even when copied faithfully from the sibling's own audit — BridgelandStability's formalization.yaml annotated its sorry as Spec.lean:52 but the live Spec.lean had grown the signature so the sorry token sat at :57; re-verify against the LIVE sibling file (not the upstream annotation) and replace bare cross-repo lines with declaration-name + an audited count field (sorry_count: 1), both drift-proof and re-verifiable. Fixed only the one adjudicated occurrence (policy §4 axis-6) and reported the unflagged sibling copy at R5:116 as out-of-scope rather than expanding the finding.
