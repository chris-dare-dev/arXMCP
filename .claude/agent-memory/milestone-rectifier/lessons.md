2026-07-12 | data-plane-governance-m1 | CLAUDE.md has an obsidian-strip clean filter (worktree +22 lines vs committed blob), so anchor on text never line arithmetic; on a pre-dirty tree, splitting git diff by a content marker unique to the foreign hunk (metadata_status=) and git apply --cached the rest gave clean hunk-scoped staging on the first pass.
2026-07-12 | data-plane-governance-m3 | Cross-repo file:line cites drift even when copied faithfully from the sibling's own audit — BridgelandStability's formalization.yaml annotated its sorry as Spec.lean:52 but the live Spec.lean had grown the signature so the sorry token sat at :57; re-verify against the LIVE sibling file (not the upstream annotation) and replace bare cross-repo lines with declaration-name + an audited count field (sorry_count: 1), both drift-proof and re-verifiable. Fixed only the one adjudicated occurrence (policy §4 axis-6) and reported the unflagged sibling copy at R5:116 as out-of-scope rather than expanding the finding.
2026-08-07 | desktop-distribution-m5 | A finding's DEFECT can be real while its named TRIGGER is not, and copying the trigger into the regression guard produces a green test that never bit: H1's "an open GET /mcp/ SSE stream makes uvicorn's unbounded drain spin forever" measured 1.15s exit either way, because sse-starlette's shutdown watcher locates the Server via signal.getsignal(SIGTERM).__self__ (we run inside Server.capture_signals()) and closes its own streams on should_exit; the shape that actually wedges an incomplete request cycle is a half-sent request (headers with a Content-Length whose body never arrives), which measured no-exit-at-60s unbounded vs exit 0 at 17.9s bounded. The tell was arithmetic, not intuition — the suite finished in 13.44s while the fix's own bound was 17s, so the guard could not have exercised it. Always run the counterfactual with the fix reverted before believing a regression guard bites.
2026-08-08 | desktop-distribution-m6 | A critic's PROPOSED FIX can carry a false factual claim even when its VERDICT is right, and copying it swaps one wrong statement for another: C1 correctly caught the README asserting "neither the production child nor the fixture spawns descendants", but its proposed replacement told me to "state the mitigation that exists - the cooperative path runs ingest_tracker.shutdown()'s SIGTERM->2s->SIGKILL". Source says the opposite in its own docstring (server/ingest_tracker.py:345, server/parse_tracker.py:301: the subprocess "receives no signal from cancelling the asyncio wrapper" and "continues running until the OS reaps it"); os.killpg exists only on per-call wall-timeout paths, never on shutdown. Verify the remediation text at source, not just the defect. Second lesson, same run: when a finding is "the probe cannot distinguish failure from absence", the fix is a POSITIVE CONTROL in the same invocation, never a better exit-code heuristic - lsof exits 1 with empty stdout AND empty stderr when broken, so I proved the counterfactual with a stub `#!/bin/sh\nexit 1` on PATH (pre-fix returned [] for a port that WAS listening; post-fix raises) before believing the guard bit.
2026-08-08 | adhoc-20260808-f62f387 | [CONFIRMED] "run the counterfactual with the fix reverted before believing a regression guard bites" — H1 here WAS that failure, filed by a critic rather than caught by me. Second, sharper lesson: when the only committed reachability path stops SHORT of the code under test, the honest fix is to extend the TEST FIXTURE, not to weaken the assertion. The fixture sidecar answered only /healthz + /readyz, so every fault arm died at the MCP smoke and no fixture-speed test could ever reach navigate_window — the critic's cheap option (assert the window-ready payload) would have looked like a fix and still passed with the gate deleted, because deleting the `match window.is_visible()` block does not change the payload the recorder writes. Teaching the fixture the three-request smoke exchange (~50 LOC, substring-matched on the exact serialized method strings — note `notifications/initialized` CONTAINS `initialize`, so the closing quote is load-bearing) made the whole window step reachable in ~1.3s instead of a real-server boot. Gate the new capability on Fault::None: serving MCP for every arm silently carries ignore-shutdown/crash-after-ready PAST the step they assert and turns their orphan-shutdown assertions into shutdown-unclean.
2026-08-09 | desktop-distribution-m9 | Two lessons, both about what a green gate actually proves. (1) A suite that pins N DECLARATIONS against each other proves only that they agree with each other — never that the shipped ARTIFACT agrees with them; the counterfactual is cheap and mandatory, and here it was buildable in 6s by pointing cargo at an out-of-tree `--config` file forcing MACOSX_DEPLOYMENT_TARGET="11.0" (the inline-table form `--config 'env.X={value="11.0"}'` is REJECTED by cargo; it must be a file). The 11.0 binary made the new test fail exactly as the implementer's warm-cache drift did. Also check the gate WIRING, not just the test: a `requires_desktop_stack` marker in a file the `make desktop-conformance` recipe never names is dead — the marker deselects it from `make test` and the recipe never opts it in, so it runs nowhere. (2) When broadening a text-honesty scanner, over-firing is as much a defect as under-firing and in the opposite direction: the README's honest sentences necessarily contain the same words while DENYING verification, so the fix needs a two-sided corpus (positives = the critic's measured bypasses; negatives = the live doc's verbatim wording) plus a test asserting the negative corpus still quotes the real file. The negation fix that worked was not "cue within N tokens" — "No component of this workspace has ever been executed on macOS 14" puts the cue 8 tokens back — but cutting the lookback at the nearest CLAUSE boundary (comma/semicolon/colon/dash + and|but|or|so|yet|because...), which keeps that exemption while breaking "There is no CI yet, and the app runs on macOS 14."
