2026-07-05 | paper-metadata-m1 | export.arxiv.org can serve sustained 429/503 (Retry-After: 0!) across independent egress IPs for 30+ min — record the exact re-run query URL in the brief and fall back to hashed official docs + offline simulation against the repo's own parser (which caught the old-style ID prefix-drop that live probing alone would have masked).
2026-07-11 | data-plane-governance-m1 | Roadmap evidence claims about code consumers go stale fast — "model_selector.py referenced only by tests" was false at research time (server/observability/__init__.py eagerly imports spend_constants -> model_selector at server startup); re-grep every consumer claim (including package __init__ eager imports) before an ADR or brief records it.
2026-07-11 | data-plane-governance-m1 | A global-gitconfig obsidian-strip clean filter makes CLAUDE.md/README.md worktree bytes differ from what git stages (frontmatter+trailer stripped at ingest) — verify sweep scope with git hash-object + diff --numstat, not the on-disk file, before advising any whole-file git add.
2026-07-12 | data-plane-governance-m3 | preflight-deviation.md written at milestone init encodes only a guess at commit scope (named R0/R3/R5 for roadmap-briefs edits) -- an exhaustive novelty-claim census found R6 needs retrofitting too, so treat such preflight notes as a starting hypothesis to verify, not a scope ceiling to trust.
2026-07-12 | data-plane-governance-m3 | stability-mflds rigor.py is a single-axis ordinal lattice (4-level Rigor enum + Certificate wrapper) for exact-arithmetic theorem-hypothesis provenance, not a multi-axis structure -- when a brief says "align trust records to rigor.py," treat it as a Certificate-shape-per-axis to reuse, not a ready-made axis set, or the policy reproduces the single-enum anti-pattern it exists to ban.
[CONFIRMED] 2026-07-12 | adhoc-20260712-955c958 | A brief's named "production sites" list for a repeated code-shape bug (kuzu.Database/Connection open+close via `del db`) undercounted by 2 — grep the exact API-call pattern repo-wide, not just the brief's file:line list; the two omitted sites included server/graph_queries.py::cite_neighbors, which is reachable from a long-running server process (not a per-invocation CLI, so the OS never releases the lock between calls) making it a live runtime bug, not just a test/ingest-script one. Independently re-derived (incl. the second omitted site, ingest/intra_paper_refs.py) via a fresh whole-repo grep in the adversarial pass of the same milestone.
[CONFIRMED] 2026-07-12 | adhoc-20260712-955c958 | kuzu==0.11.3's Database and Connection both implement .close() (idempotent, forces native release rather than waiting on GC) AND __enter__/__exit__ (confirmed byte-identical vs GitHub tag v0.11.3 source via gh api + sha256, not just the installed .venv copy) -- when a brief only proposes `del db`, check the installed package for a documented close()/context-manager surface before assuming a hand-rolled fix is the only option. Independently re-derived by reading .venv/Lib/site-packages/kuzu/{connection,database}.py directly in the adversarial pass of the same milestone; database.py's own close() docstring additionally confirms conn-before-db ordering is a documented precondition, not just an empirical Windows quirk.
2026-07-12 | adhoc-20260712-955c958 | When a brief lists N production-fix-sites to unblock M skip-marked tests, trace each test's OWN body plus every fixture in its parameter list for the same bug shape before trusting the site list is complete — here only 1 of 8 target tests depended on the 5 named production sites alone; the other 7 also needed test-body or shared-fixture (`populated_db`, unnamed anywhere in the brief) fixes, and one test never called any of the 5 named functions at all, so the production fix alone would leave 6 of 8 tests still failing after the skip decorator was removed.
2026-07-12 | adhoc-20260712-698fead | Write tool is sandboxed to the researcher's own worktree even when the orchestrator's OUTPUT PATH is an absolute main-repo path outside it ("Edit the worktree copy... instead of the shared-checkout path"); write to the worktree-mirrored path then `cp` via Bash (unsandboxed) to land it at the literal path requested -- the same fallback is needed for agent-memory files when the worktree's copy predates them (mine only had a stale MEMORY.md, no lessons.md/anti-patterns.md).
2026-07-12 | source-truth-spike-1 | `tools/_arxiv_api.py`'s Atom `/api/query` client carries ZERO license info for ANY id shape -- live-tested a 30-paper mixed sample (new/old/versioned, both notebooks): no `arxiv:license` element, no `rel="license"` link, confirmed by enumerating all 17 distinct tags in the raw response (schema-level absence, not per-paper luck). arXiv's OAI-PMH endpoint (`oai2?verb=GetRecord&metadataPrefix=arXiv`) DOES carry a `<license>` element with a real URI, verified live for the same paper Atom returned blank for -- when a roadmap brief assumes "the arXiv metadata API returns license URIs," check WHICH of arXiv's several API surfaces (Atom search/id_list vs OAI-PMH vs export.arxiv.org e-print) actually carries the field before scoping a milestone to the client already in the repo.
2026-07-12 | source-truth-spike-2 | A pooled/aggregate coverage % over many small units can hide a total-loss failure concentrated in one unit -- measuring printed-theorem-number recoverability from `ltx_tag ltx_tag_theorem` markup across 20 Bridgeland papers gave 99% raw / 96.6% hand-corrected pooled coverage (comfortably >80%), but one old-style 1996 paper (`alg-geom/9606006`) had its entire ~25-theorem population invisible to that markup path (rendered as plain bold paragraph text, "3.2. Theorem" number-before-keyword order, zero `ltx_theorem` divs) -- the extractor's own denominator was silently wrong (0, not 25), only caught by an independent cross-check that searched the FULL document for bold theorem-style labels regardless of enclosing markup rather than trusting the primary extractor's element-selection criterion. When hand-validating an extractor against a hand count, always test whether the denominator itself is right (a second, structurally-different detection method), not just whether the numerator regex looks plausible on the denominator the extractor already found -- also caught a live regex bug (appendix-lettered "Theorem A.2" misclassified as unnumbered, ~10% of one paper) that only surfaced from eyeballing raw "UNNUMBERED" output before trusting any aggregate number.
2026-07-13 | source-truth-m1 | Before designing a "new" arXiv API client from a task brief's literal spec, grep the repo for existing clients against the SAME external service first -- `ingest/oai_delta.py` (E11_S02) turned out to be a fully-built, already-adversary-critiqued OAI-PMH client (503/Retry-After exponential backoff, redirect-pinning, and the CURRENT canonical `https://oaipmh.arxiv.org/oai` host) that the brief's literal `export.arxiv.org/oai2` text never referenced; arXiv's own OAI-PMH `<request>` response element echoes the base URL that actually served the request, which is how the endpoint drift surfaced. Separately, don't let resolving one caveat imply a related one is also resolved: OAI-PMH `GetRecord`'s identifier FORMAT resolving cleanly for old-style ids (`oai:arXiv.org:math/0212237`, HTTP 200, correct record) does NOT mean license DATA is present -- live-verified 2/2 old-style papers (1994 and 2002 submissions) have no `<license>` element at all under either `arXiv` or `arXivRaw` metadataPrefix, while a 2020 new-style paper has one. Format-works and data-exists are independent claims requiring independent verification.
2026-07-12 | source-truth-m1 | Before recommending "build a new OAI-PMH client" off a spike's finding, grep the repo for existing OAI-PMH code first -- `ingest/oai_delta.py` (E11_S02, production, tested) already talks OAI-PMH, but against the CURRENT endpoint (`oaipmh.arxiv.org/oai`, migrated off `export.arxiv.org/oai2` in March 2025 per its own comment) and a RICHER metadata prefix (`arXivRaw`, not the `arXiv` prefix the spike live-tested), shaped for date/set-windowed discovery (`ListRecords`) rather than per-known-id lookup (`GetRecord`, unused anywhere in the repo) -- so the spike's live-verified license-field result doesn't actually transfer to the endpoint+prefix combo the codebase's own existing client uses; that module's claim that `arXivRaw` carries `<license>` is only a code comment, never independently checked. Also: grepping `var/arxmcp/corpus/raw/` vs `corpus/parsed/` directory listings (not just file contents) surfaced that old-style-id raw sources are silently absent corpus-wide (ar5iv-cache-hit path never persists them) -- when a field's source is "the fetched X under path Y", confirm Y is actually populated for every id *shape* in scope, not just spot-check one example the task hands you.
2026-07-13 | source-truth-m2 | The shipped backfill precedents (tools/notebook_metadata_backfill.py, tools/notebook_documents_backfill.py) both write to brand-new, separate per-notebook SQLite stores -- neither mutates an existing LanceDB table's rows, so neither is a mechanism precedent for a backfill that must hydrate NEW COLUMNS on ~15K EXISTING chunk rows without re-embedding. merge_insert(...).when_matched_update_all() (the only LanceDB writer this repo uses today) replaces the WHOLE matched row and requires a populated embedding for every row (ingest/store.py::_build_arrow_table raises otherwise) -- the wrong tool for a column-only backfill. lancedb.table.Table.update(where=..., values={...}) (confirmed via live inspect.signature introspection on the installed lancedb==0.30.2) updates only the named columns and never touches unrelated columns like embedding_stmt/embedding_proof, but is used nowhere in this repo (grepped ingest/, zero hits) and applies one static values dict per where-matched set, so per-chunk-unique backfill values still need ~15K individual calls or a read-modify-write batch -- neither cost has been measured. When a milestone brief cites an existing "backfill precedent," verify it actually mutates the SAME store the new work targets before assuming the mechanism transfers.
15. 2026-07-13 | source-truth-m2 | Resolved the sibling brief-1's open "how do we backfill per-row-unique values into an existing LanceDB table" question with two live throwaway-table tests (never the live corpus): (1) merge_insert(...).when_matched_update_all() with a PARTIAL-column source batch (pk + 2 of 4 target columns, omitting body_text) hard-fails -- `RuntimeError: lance error... Spill has sent an error` at the DataFusion/Lance layer, NOT a silent null-out of omitted columns -- the source batch must carry the table's FULL schema or the call refuses outright. (2) Table.update(values_sql={...}) rejects a `CASE chunk_id WHEN 'a' THEN ... END` expression keyed on the pk -- `RuntimeError: ...not supported SQL in lance` -- so there is no way to encode N distinct per-row values into one call; bulk use would cost one .update() call PER ROW. The actual already-shipped answer neither spike nor sibling brief-1 cited: `ingest/embed_equations.py:82-139` (production code, the `equations` table's `embedding_eq` backfill) does read-modify-write -- `table.to_arrow().to_pylist()` the whole table, mutate ONLY the target key(s) in each Python dict (every other key incl. embedding columns passes through byte-identical), `pa.Table.from_pylist(rows, schema=<full schema>)`, then `merge_insert(pk).when_matched_update_all().when_not_matched_insert_all()` the FULL reconstituted batch. Before concluding a backfill mechanism is unprecedented in this repo, grep for merge_insert/`.update(` against EVERY table the same schema module (ingest/schema.py) defines, not just the target table -- the closest precedent may be a sibling table's backfill, not the target table's own history.
16. 2026-07-13 | source-truth-m5 | A milestone brief's "hydrated on both live notebooks" claim needs literal on-disk verification, not trust: live-opened all 5 notebook LanceDB tables (`.venv/Scripts/python.exe` + `lancedb.connect`) and found `bridgeland-stability` (15,106 rows) + `fourier-duality` (4,475 rows) = 19,581 rows ARE on the v2 26-col schema and fully hydrated, matching the roadmap number -- but `bridgeland-stability-pdfs` (780 rows) and `fourier-duality-pdfs` (2,051 rows), both live and queryable via get_chunk TODAY, are still on the pre-migration v1 21-col schema entirely (columns absent, not null). Separately and more load-bearing: `server/handlers/chunk.py`'s LanceDB read (`chunks_table.search().where(...).limit(1).to_arrow()`, no `.select()`) does full-column projection by default -- confirmed by reproducing the exact query live against both an hydrated and an unmigrated table -- so `server/corpus.py` (which only opens/version-pins the table, no projection logic at all) needs zero changes when new columns are added to the schema; the ONLY code that determines what surfaces is the handler's dict-literal field list. But that same no-`.select()` behavior means any new field added via `row[col]` (matching the existing bracket-index style for all 11 current fields) KeyErrors on a table missing the column -- `row.get(col)` is required, not a style nicety, whenever a schema migration has landed on some but not all live notebooks. Before trusting a brief's "field X is already hydrated everywhere" framing, open every on-disk table the serving code can reach (not just the ones a prior milestone's smoke-test targeted) and reproduce the exact serving-code query against the least-migrated one.
17. 2026-07-13 | source-truth-m3 | A roadmap item's own `links.code` list can point at the wrong file even one level of naming collision away: `plans/source-truth/roadmap.yaml`'s source-truth-m3 entry names `server/resources.py` as a target, but that module is the `Resources` process-lifecycle dataclass (BGE-M3/LanceDB/cache singletons attached to `app.state.resources`, E06_S01) -- the ACTUAL MCP `resources/list`/`resources/read` surface (where `arxmcp://notebooks` is registered via `@mcp_server.resource(...)`) lives in the similarly-named but distinct `server/mcp_resources.py`. The roadmap's second linked file, `server/paper_metadata_store.py`, is also not a literal edit target -- it's the per-notebook-SQLite-store PATTERN that `server/documents_store.py` (the actual m1 registry, and m3's real data source) already mirrors. Separately, confirmed live on disk that only 2 of 5 `var/arxmcp/notebooks/<slug>/` dirs have a `documents.db` (bridgeland-stability, fourier-duality -- matching `tools/documents_coverage_report.py`'s hardcoded `DEFAULT_NOTEBOOKS`), so any manifest/aggregation milestone enumerating "all notebooks" via `NotebooksStore.list_notebooks()` will hit un-hydrated notebooks and needs an explicit degrade path, not an assumption of universal coverage. When a roadmap brief names file targets, verify each by reading its actual module docstring/first-line purpose before treating the name as authoritative -- two of two links were misleading in this case.
2026-07-13 | source-truth-m3 | `server/tools.py::wrap_retrieved_text`'s `kind=` dispatch is a CLOSED dict (`{"equation": _WRAP_TAG_EQUATION, "notebook": _WRAP_TAG_NOTEBOOK}.get(kind, _WRAP_TAG_CHUNK)`, `:574-577`) -- any `kind` value not already a dict key (e.g. a naive `kind="manifest"` from a new resource) SILENTLY falls back to `<retrieved_chunk>` wrapping instead of raising or producing a distinct tag. Any future milestone adding a new wrapped-content surface (a new MCP resource or handler) must add BOTH a new `_WRAP_TAG_*` constant AND a new dict entry before calling `wrap_retrieved_text(text, kind=<new>)`, or the payload mislabels itself with the wrong delimiter tag with no error anywhere. Separately, when designing a self-referential content-hash field (a hash embedded inside the object it describes, e.g. a corpus-manifest resource), nest the hashed content under its own sub-key (`{"content_hash": ..., "snapshot": {...only this is hashed...}}`) rather than "exclude these N keys by name" from a flat object -- a structural hash boundary can't be gotten wrong by a future field addition forgetting to join an exclusion list, and read-time/wire-format fields (`generated_at`, a schema-version int) must stay OUTSIDE the boundary or the hash changes on every read even when the underlying data hasn't. Also confirmed independently (matches this same milestone's prior lesson entry): `fourier-duality`'s documents-registry row count (52) and its `corpus-version.json` `paper_count` (51) already legitimately diverge by one (a registered-but-not-yet-chunked paper) -- when a design aggregates counts from two independently-written stores, report them as separate fields and never assert/reconcile equality; a one-off drift between independently-sourced counts is expected, not a bug to design around.

2026-08-03 | verification-contract-m1 | A milestone brief line-number citation ("previously 290-298") can itself be stale relative to the roadmap.yaml evidence block ("717-733, verified 2026-08-03") if the axiom_audit block landed in between -- always re-grep the literal string (here `"ok"` in the status computation) rather than trusting either citation, and separately verify the handler docstring version number against the actual schema file (lean_verify.py:9 said "version 12" while server/schemas/lean_verify_result.json was live at version 22 -- a docstring can drift independently of the file it describes).
2026-08-03 | ui-uplift-m6 | A milestone brief for a CSS custom-property re-derivation (app.css :root token block) only lists app.css in the roadmap's links.code, but tests/test_ui_m3_dark_and_htmx_feedback.py hard-pins the EXACT hex values of two in-scope tokens via regex/substring asserts (dark --border must equal "#6e7681", dark button-text-compensation must equal "#0d1117") -- grep every test file for the literal hex string of each in-scope token before scoping the diff, not just the files the roadmap item names; the same file also has several structurally-safe sibling tests (token-name-presence, var(--x)-reference checks) that will NOT break, so distinguish the two rather than assuming the whole test file is at risk.
2026-08-03 | ui-uplift-m6 | www.w3.org (WCAG spec + WAI wiki) returns HTTP 403 to a non-interactive `curl` fetch even with a standard browser User-Agent (Cloudflare managed-challenge page, confirmed by inspecting the response body) -- the WebFetch tool itself still reads it fine, so the content/quotes are obtainable, but an independent sha256-of-raw-bytes pin is NOT, unlike MDN/GitHub Pages/most other doc sites which curl cleanly. When a brief needs a hashed pin for WCAG/W3C-spec facts, budget one source slot for a curl-able corroborating secondary source (MDN's accessibility docs, WebAIM, an official GitHub-hosted mirror) and state the w3.org access limitation transparently in the brief rather than fabricating a hash or silently dropping the primary citation.
2026-08-04 | adhoc-20260804-c8e6048 | lean_verify.py's six declaration-site regexes are ALL `.match()`-anchored (never `.search()`), which is what makes Lean `--` comments invisible to `_declaration_names` today (emergent from anchoring, not a deliberate comment-stripper) -- before recommending a broader `.search()`-based "keyword anywhere in the line" fix to a line-based extractor in this repo, live-test it against a comment line containing the target keyword; a naive broadening false-positived on `-- prove this theorem using induction` and `-- def foo does something`, while a narrower `\bin\b` + optional-modifiers + keyword match (mirroring Lean's own `<modifier> in <command>` grammar) caught the real set_option/open bug shapes (issue #382) without the regression.
2026-08-04 | adhoc-20260804-c8e6048 | leanprover.github.io/reference/*.html is the STALE Lean 3 manual (self-titled "3.3.0" in its own page title) despite generic-sounding chapter titles like "Other Commands" that read as Lean-4-relevant in search snippets -- WebFetch happily returns real, confident prose from it that is simply wrong for Lean 4. The current Lean 4 manual lives at lean-lang.org/doc/reference/latest/<Title-With-Dashes>/ (resolve the exact slug from a search-hit's own title, e.g. the "Namespaces and Sections" hit -> .../Namespaces-and-Sections/; guessed slugs like .../Other-Commands/ 404). Any Lean-4-grammar claim is worth cross-checking against the primary parser source (github.com/leanprover/lean4, src/Lean/Parser/Command.lean) before citing it -- that source is also usually MORE precise (exact grammar + docstring) than either manual.

2026-08-04 | ui-uplift-m7 | `https://api.webstatus.dev/v1/features/<id>` returns the machine-readable Baseline record (`baseline.status` = widely/newly/limited, plus `low_date`/`high_date` and per-engine versions + WPT scores) as plain JSON that curls cleanly and hashes -- this CLOSES the gap the m6 lesson recorded, where w3.org/MDN Baseline facts could not be sha256-pinned because w3.org 403s a non-interactive curl. Feature ids are kebab-case and NOT always the property name: `clamp()` is `min-max-clamp`, `text-wrap: balance` is `text-wrap-balance`. Live results 2026-08-04: min-max-clamp/font-variant-caps/font-variant-numeric all `widely`; `text-wrap-balance` `newly` (low 2024-05-13 -> Widely exactly low+30mo = 2026-11-13, which independently REPRODUCED the number the repo's own 2026q3 final-report.md asserted); `text-wrap-pretty` `limited` (Firefox absent, WPT 0.0). Prefer this API over prose-doc fetches for any Baseline-gated milestone. Separately and repo-specific: server/frontend/static/app.css is at 471 of a HARD 480-line cap asserted in THREE test files (test_ui_m3_dark_and_htmx_feedback.py:571, test_ui_m4_in_place_add_paper.py:695, test_ui_m5_create_remove_in_place.py:816) whose own messages say all three "MUST move in lockstep" -- grep the cap before scoping any CSS milestone, because a 9-line budget, not the CSS itself, is the binding constraint; the pyproject `*.css` glob and `COPY server/` already cover a future tokens.css split, so the documented escape hatch costs no packaging change, only the APP_CSS fixture in ~7 test modules.
2026-08-04 | ui-uplift-m7 | A repo-wide "extend the existing :root block" acceptance criterion can be blocked by a test that never mentions the new token type: tests/test_ui_contrast.py::test_all_colour_tokens_are_oklch_on_one_of_two_hues iterates EVERY raw :root token and skips only `--mono` and `--dur-*`, so any non-colour token (font/size/tracking) hard-fails on `is not an oklch() value` — before scoping a token-adding milestone, grep the test suite for loops over the token block itself, not just for the literal values of in-scope tokens (the m6 lesson's hex-grep is necessary but not sufficient). Separately: app.css's line cap is asserted by three test files in lockstep AND the file was at 471/480 (9 lines) at research time, so the cap raise is step one of the milestone, not a cleanup at the end.
2026-08-04 | ui-uplift-m7 | When a milestone must apply a token to "every X surface", enumerate the Python HTML-fragment builders separately from the Jinja2 templates — they drift: server/routes/notebooks.py::_paper_row_html:2023-2024 emits bare `<td>{paper_id}</td>` / `<td>{added_at}</td>` while notebook_detail.html:325-326 renders the SAME table rows with `<code>`/`<time>`, so an htmx-appended row renders in a different font+figure style than its neighbours until reload; its sibling _notebook_row_html:2081-2083 got it right, which is why a spot-check of one builder would have missed it. Also check whether a CSS token is applied by SELECTOR or merely implied: `--mono` is applied at exactly 4 selectors (app.css:121,167,199,213) and `table code` is table-scoped, so every `<code>` outside a table silently falls back to the UA generic `monospace` — reading "<code> means mono" off the markup under-scopes by ~10 sites.

2026-08-04 | ui-uplift-m8 | When a milestone DELETES a CSS primitive, the binding constraints are not the primitive rule but (a) the compound descendant selectors that carry BAN-R2 coverage for server-emitted classes -- simulating the deletion by stripping every .card-bearing rule and re-running tests.test_ui_class_css_coverage._css_defines_class showed hint/empty/display-name flip from covered to UNCOVERED, a hard failure -- and (b) the SECOND, non-lockstep line cap: app.css sits at 595/600 (3 lockstep files) but tokens.css is at 198/200 asserted by ONE test (test_ui_m7_type_scale.py:426), and a milestone adding new --rule-* tokens hits the tokens cap first; the m7 split escape hatch is spent (test_tokens_css_declares_only_root_blocks forbids rules there). Also re-confirmed the m7/m10 pattern a third time: the roadmap summary dropped authored values that exist in the discovery tree (art-direction-scout-brief.md:163-177 names --rule-section/--rule-row/--rule-meta AND the full/60%/dotted grading), and the May-2026 UPL-2 (:focus-visible rules) still outranks the 2026q3 UPL-2 in grep order. Highest-value measurement: re-grounding a token from --card-bg to --bg can silently miss its OWN derivation target while passing the registry floor (light --fg-muted 7.0148 -> 6.8230 against a declared 7.00 AAA target, gate is 4.5), and re-basing a color-mix ground can push the registry TIGHTEST pair under its floor (light --border on tbody tr:hover 3.0401 -> 2.9533, SC 1.4.11 fail) -- always re-measure derived mix/fade constants, not just literal grounds.
2026-08-04 | ui-uplift-m8 | The 'UPL-N' id collision between arXMCP's two UI runs is tool-dependent, not absolute: from repo root `grep -rn "UPL-2" .` returns the CURRENT plans/ui-uplift/roadmap.yaml:331 first, but `rg -n "UPL-2"` (what the Grep TOOL uses) returns tests/ first and the whole first page is May-2026 material -- including tests/test_ui_a11y_baselines.py, a PASSING test whose docstring defines UPL-2 as ':focus-visible outline-ring rules', i.e. a shipped feature that is not this milestone at all. When two planning runs share a candidate-id namespace, state WHICH search tool surfaces the decoy rather than asserting a single grep order, and anchor the implementer on the roadmap.yaml file:line. Separately, api.webstatus.dev closed every Baseline question for a rule-ladder milestone with zero refusals -- :has() (Widely 2026-06-19) and CSS Nesting (Widely 2026-06-11) BOTH crossed the 30-month bar within the prior 8 weeks, so a memory-based judgement would very likely have refused two features that now ship; always query the API rather than recalling a status. And www.w3.org returned HTTP 200 to a plain curl -A Mozilla/5.0 today, NOT the 403 the ui-uplift-m6 lesson recorded -- treat that lesson as conditional/intermittent, not settled.

2026-08-04 | ui-uplift-m12 | A milestone that NESTS existing top-level blocks (five <div>s into a <details>) silently breaks CSS that no test covers: app.css:64-65's rule ladder is `main > :where(section, div) + section|div` — a DIRECT-CHILD selector whose `main >` combinator m8's rectify comment explicitly calls load-bearing — so every re-parented block loses its border/margin/padding and `main > ... + details` matches nothing. `test_the_ladder_is_horizontal_only` checks the ladder's DIRECTION, not its COVERAGE, so this ships as a silent visual regression. Before scoping any milestone that changes DOM DEPTH (not just order), re-read every structural selector for `>` combinators and ask which ones stop matching — grepping for the moved elements' class names finds nothing because the selectors are element-based. Cheapest fix is folding the new element into the existing selector list (`+ :where(section, details)`) for 0 net lines, which matters because app.css was at 598/600.
2026-08-04 | ui-uplift-m12 | Verify a poll-visibility premise at the vendored source, not from memory: htmx 2.0.10's polling scheduler (minified `ut` in server/frontend/static/htmx.min.js) guards ONLY on `bodyContains(elt)` + a cancelled flag — no offsetParent, no IntersectionObserver — so a `hx-trigger="every 2s"` element inside a CLOSED <details> keeps polling invisibly, confirming the AC's premise. Grepping the minified bundle with shell regex catastrophically backtracks (a `grep -o` with nested quantifiers hit the 120s timeout); use python3.12 + str.find/re.finditer on the file instead. Also: the repo already had a RECORDED REJECTION of <details> under a poll (server/routes/ui.py:280-286, pinned by tests/test_m3_endpoints.py:529-531) that points the OPPOSITE way to the new milestone — it does not apply only because the polled element is a DESCENDANT of the disclosure rather than containing it, which is a constraint to carry forward, not a difference to wave away. Search for prior REJECTIONS of a technique, not just prior usages, before treating a brief's "already in the product" framing as precedent.
2026-08-04 | ui-uplift-m12 | A roadmap summary's block/form counts can be a mis-transcription of a COMMENT rather than a count of elements: the brief said "five mutation forms", the template has SIX <form>s, and the "five" traces to notebook_detail.html:8-12's m8 note saying "the five job forms drop to DIV" — which counts BLOCKS, not forms. The uncounted sixth (rename, inside the LEADING identity section) is what makes AC#1 ("visible without scrolling past ANY input form") and AC#4 ("the FIVE moved forms") mutually inconsistent. Separately, a stale per-block px measurement is worth RE-DERIVING rather than discarding: the discovery's y=1823 survived m7/m8/m10 to ~1740 because m8's rule ladder made five `+div` boundaries TIGHTER (41px) while only one `+section` boundary grew (65px), netting -30px — and the m8 critique's widely-quoted "+96px added to the detail page" describes the pre-rectify uniform-4rem design that never shipped (app.css:59-63 says so explicitly). Recompute from the shipped CSS before quoting either number.
2026-08-04 | ui-uplift-m12 | [CONFIRMS the ui-uplift-m8 lesson and supplies its MECHANISM] The "UPL-N decoy is tool-dependent" effect is not about match ranking — `.claude/` is a DOT-DIRECTORY and `rg` skips hidden paths by default, so a recursive `rg` from repo root cannot see the frontend-uplift design tree AT ALL: `grep -rn "UPL-1\b" .` returns 144 matches with the real `plans/ui-uplift/roadmap.yaml:441` FIRST, while `rg -n "UPL-1\b" .` returns only 6 and its first hit is the May-2026 DECOY comment in `server/frontend/static/app.css:432` (the prefers-reduced-motion gate) — i.e. the decoy is in the very file the milestone edits, and `.claude/roadmap/ui-attractive-polish-roadmap.md` is invisible despite `git check-ignore` exit 1 (NOT gitignored). `rg --hidden --no-ignore` restores all 144; rg reads `.claude/` fine when given an explicit path argument. Always state the flag, not just the tool, and anchor implementers on `plans/<slug>/roadmap.yaml:<line>` plus explicit `.claude/notes/frontend-uplifts/` paths. (Appended rather than editing the m8 line in place: CLAUDE.md §3 documents 2-3 concurrent agent sessions on this box, so an in-place rewrite of a shared memory file is a lost-update risk.)
2026-08-04 | ui-uplift-m12 | A roadmap's Baseline justification can be STALE IN THE BROWSER'S FAVOUR and the AC still correct for a different reason — m12 asserts animating `<details>` needs `interpolate-size` or `::details-content`, "both Chromium-only", but api.webstatus.dev on 2026-08-04 says `details-content` is **newly** since 2025-09-16 with Chrome 131 + Firefox 143 + Safari 18.4 and WPT 1.0 stable in EVERY engine (only `interpolate-size` and `calc-size` are still `limited`/Chromium-only); the refusal survives because Newly != Widely (Widely = low+30mo = 2028-03-16), which is the same basis as m6/light-dark(), m7/text-wrap:balance and m10/line-clamp — so re-query every Baseline claim and RESTATE the reason rather than repeating the roadmap's wording into a commit, which would ship a checkable falsehood. Feature ids that resolve: `details-content`, `calc-size`, `interpolate-size`, `transition-behavior`. Repo-specific and now THIRD consecutive: `server/frontend/static/app.css` was at 598 of the hard 600 cap (3 lockstep test files) and `tokens.css` at 289/290 — the line budget, not the CSS, is again the binding constraint. Highest-value single find: the detail route ALREADY passes `latest_run` (`server/routes/ui.py:461`) from the SAME `store.get_latest_ingest_run(slug)` the polling endpoint calls (`server/routes/notebooks.py:2283`), so an AC demanding a cue "from the same row the fragment reads" cost zero new queries — always check whether the template context already carries the row before scoping a route change.
2026-08-06 | desktop-distribution-m1 | When a single-root confinement AC meets independent legacy path overrides, enumerate every override and force an explicit precedence/compatibility decision before centralizing derivation.
2026-08-06 | desktop-distribution-m1 | Before researching a milestone that depends on a spike, verify both the spike's issue state and its local ADR/artifact first; an open dependency with no output makes downstream implementation a NO-GO even when the milestone brief is otherwise precise.
2026-08-06 | desktop-distribution-spike-2 | A container that relies on WORKDIR-relative data defaults will silently leave its mounted volume when installed fallback moves to a platform-native root; set ARXMCP_DATA_DIR explicitly to the mount target before changing fallback semantics.
2026-08-06 | desktop-distribution-spike-2 | Proving one application-data root requires inventorying library-owned writes too: Starlette multipart spooling, Hugging Face/XDG caches, and MinerU's inherited HOME escape an otherwise-correct first-party path resolver unless the launcher redirects them before imports.
2026-08-06 | desktop-distribution-spike-1 | A bootstrap /healthz smoke deliberately skips model/native initialization, so frozen-distribution evidence must include a separate offline model-construction and representative native-operation probe; successful server boot alone does not prove the bundle's ML closure.
2026-08-06 | desktop-distribution-m2 | A bootstrap /healthz boot creates the notebook registry but skips retrieval-cache and corpus-marker writers, so relocation smokes must exercise each named writer explicitly before claiming one-root confinement.
2026-08-06 | desktop-distribution-m2 | A relocation smoke must create the venv at its final path and install the wheel there—Python venvs have absolute shebangs and are not themselves relocatable.
2026-08-06 | desktop-distribution-spike-3 | On macOS, proving supervisor-crash cleanup requires a child-observed kernel lifetime lease such as stdin-pipe EOF; Tauri exit callbacks cannot run after SIGKILL, and free-port bind-close-rebind is inherently racy.
2026-08-06 | desktop-distribution-spike-3 | A desktop supervisor must budget beyond the server's internal 30-second drain and cannot rely on Rust Drop cleanup because Tauri App::run terminates the process directly.
2026-08-07 | desktop-distribution-m3 | Forward-compatible minor protocol changes need a strict versioned core plus an explicit extensions object; copying deny_unknown_fields wire structs makes the compatibility promise impossible.
2026-08-11 | desktop-distribution-m10 | validate_plan (apps/desktop/crates/supervisor/src/main.rs) checks !smoke against five test-only knobs but child_argv.is_empty() is a separate, always-active rule -- a self-authored-plan test for "refused under every validate_plan rule" must exercise both branches, not just the smoke-knob one the brief names; separately, tauri.conf.json has bundle.active:false with no resources/externalBin key, so "the bundled frozen child" this milestone assumes as input is not yet wired by any prior desktop-distribution milestone (m7 built the PyInstaller artifact but never touched tauri.conf.json).
2026-08-11 | desktop-distribution-m15 | m10's own doc comment on child_payload_root() (main.rs:307-310) already states precisely what changes for .app assembly ("only this function changes") -- more precise than the roadmap AC's "re-point child_payload_root() and resolve_inside()" wording, which over-scopes: resolve_inside() is generic root/candidate containment logic with no sibling-directory assumption baked in and needs no code change, only a different root argument from its caller. Also: tests/test_desktop_support_floor.py's minos check reads ONLY the two Rust binaries (DESKTOP_SUPERVISOR_BIN, ARXMCP_FIXTURE_SIDECAR) via _BINARY_ENV -- the PyInstaller-produced executables' own minos (from whatever CPython uv resolves) is unpinned and unchecked anywhere, unlike the Rust side's .cargo/config.toml MACOSX_DEPLOYMENT_TARGET pin; a roadmap AC saying "both binaries' minos" needs disambiguating which two binaries it means before scoping the check.
2026-08-11 | desktop-distribution-m11 | operator_settings.py's default DB path (_resolve_db_path -> ApplicationPaths.resolve().notebooks_db = <data_root>/cache/notebooks.db) is INSIDE the data root, so it structurally cannot be the store of record for "which root did the operator pick" -- a brief claiming "no new store is introduced" for a data-root-selection milestone should be treated as a brief error and flagged, not force-fit; also apps/desktop/crates/supervisor/src/main.rs's self_authored_plan has zero ARXMCP_DATA_DIR (or any override) read anywhere -- grep every env::var/lookup( call site before assuming an existing Python-side env override "reaches" the Rust self-authoring arm.
2026-08-11 | desktop-distribution-m11 | Rust main() (main.rs) resolves data_root via load_plan() at line ~667, BEFORE tauri::Builder::default() is constructed at line ~716 -- no Tauri app/window instance exists yet at the point a data-root decision must be made, so tauri-plugin-dialog (which only exposes calls on App/AppHandle) cannot be the first-run picker without restructuring main()'s sequencing; a pre-Tauri native crate (e.g. rfd) is the only path found that needs no restructuring. Also: this app carries no App Sandbox entitlement anywhere under apps/desktop/ (no .entitlements file, no com.apple.security.* key in tauri.conf.json), so security-scoped bookmarks -- which exist solely to solve sandboxed apps' lost access on relaunch -- are the wrong mechanism here; check for a sandbox entitlement before reaching for bookmark APIs on any macOS persistence question in this repo.
