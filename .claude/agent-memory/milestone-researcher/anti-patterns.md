stale-consumer-claim | a brief/roadmap evidence line asserts "referenced only by tests" or similar import/consumer claims | re-run the import grep across server/, tools/, tests/ including package __init__ eager-import chains before recording it
stale-consumer-claim | a brief/roadmap evidence line asserts a module is 'referenced only by tests' to justify moving/deleting it | grep the module's import path across all *.py (server/ especially) before drafting the disposition — arXMCP precedent: model_selector had a live consumer at server/observability/spend_constants.py:51
scope-list-undercounts-pattern-sites | a brief gives an explicit "sites: file:line, file:line..." list for a structural/repeated bug (same API-call shape reopened across files) rather than "search for pattern X" | grep the exact code signature (e.g. `kuzu.Database(` + `del db`) across the WHOLE repo (ingest/, server/, tools/, tests/), not just the brief's named files — cross-check the hit count against the brief's claimed count; server/ hits are highest-value since a long-running server process (unlike a per-invocation CLI) makes the same bug reachable at runtime — arXMCP precedent: adhoc-20260712-955c958's brief named 5 kuzu del-db sites, repo grep found 7, the 2 omitted included the live cite_neighbors MCP-tool path
flat-guard-skips-second-close | a proposed/actual `finally` block cleans up two dependent resources sequentially -- `if a is not None: a.close(); b.close()` -- rather than nesting | if `a.close()` can raise (it usually forwards to a native/library call that isn't provably total), nest it: `finally: try: (if a is not None: a.close()) finally: b.close()`, or use the resource's own context-manager protocol if `hasattr(cls, "__enter__")` -- arXMCP precedent: adhoc-20260712-955c958's own milestone brief proposed exactly the flat (unsafe) shape for kuzu conn.close()/db.close(); a flat guard silently skips db.close() (leaking the lock) whenever conn.close() raises, which is worse than the status quo bug it's fixing
unnamed-blocker-in-unskip-brief | a brief proposes removing a skip-marker from M tests after fixing N named production sites | grep the skip marker's every decorator usage, then read each decorated test's full body plus every fixture in its parameter list for the same code shape the brief targets, not just the N named sites -- arXMCP precedent: adhoc-20260712-955c958 named 5 production del-db sites but only 1 of 8 skip-marked tests depended on production sites alone; the other 7 needed test-body or fixture (`populated_db`, unnamed in the brief) fixes too, and the ONE helper the brief did name (`build_synthetic_kuzu_graph`) turned out to be used by zero of the 8 target tests
kuzu-del-db-grep-false-positive | grepping `del db` repo-wide to enumerate kuzu lock-bug sites | verify each hit's `db` local is actually bound to `kuzu.Database(...)`, not e.g. `lancedb.connect(...)`, before counting it -- arXMCP precedent: tests/test_proof_chain.py:205's `del db` is a LanceDB connection (unrelated F4-fix comment for GC-ordering flakes), not a kuzu site, even though the same file has a real kuzu `del db` site 90 lines earlier (L116)
regression-test-fixture-shares-target-bug | a new regression test for bug X is planned to reuse an existing test fixture that performs the same buggy operation (X) during its own setup/teardown | convert the fixture's own teardown to the fix too (or flag it explicitly as a confound), otherwise a pass conflates "fixture's leftover state happened to clear in time" with "the code under test is fixed" -- arXMCP precedent: adhoc-20260712-698fead's planned cite_neighbors double-reopen regression test would reuse tests/test_graph_queries.py::kuzu_db, whose own `del db` teardown (L93) carries the identical kuzu Windows-lock bug being tested

hand-pinned-version-literal-hides-in-test-body | a milestone bumps a global schema/version constant (e.g. TOOL_SCHEMA_VERSION) that other test files echo via a dynamic symbol reference | grep for a HARDCODED integer literal comparison against the constant (e.g. `assert TOOL_SCHEMA_VERSION == 22`) in addition to symbol-based checks -- arXMCP precedent: tests/test_handlers_lean_verify.py:224 pins the literal 22 inline (with a docstring history list to extend) even though every other cross-check in the suite references the TOOL_SCHEMA_VERSION symbol dynamically and needs no edit; missing the one hardcoded site is easy because it looks identical to the dynamic ones at a glance.
css-token-value-pinned-in-unrelated-test | a milestone re-derives named CSS custom-property values (a :root token block, e.g. app.css) | grep tests/ for the CURRENT literal hex string of every token in scope (not just "token name present" or "var(--x) referenced" structural checks) before scoping the diff -- arXMCP precedent: ui-uplift-m6's OKLCH palette re-derivation breaks two assertions in tests/test_ui_m3_dark_and_htmx_feedback.py (test_dark_border_uses_corrected_hex_not_primer_canonical pins dark --border == "#6e7681"; test_dark_block_corrects_button_text_color pins the button-text compensation == "#0d1117"), neither named in the roadmap item's links.code. Same underlying shape as hand-pinned-version-literal-hides-in-test-body generalized from integer schema versions to CSS hex literals -- also watch for a hardcoded literal standing in for a token's value inside a TEST's own arithmetic (not just its assertions), e.g. tests/test_ui_m5_create_remove_in_place.py:600's `canvas = "#0d1117"` duplicates dark --bg as a Python string for a WCAG contrast calculation, which goes silently stale (wrong answer, not a failure) if --bg changes without the test being updated in lockstep.
w3org-cloudflare-blocks-curl | attempting to `curl`/non-interactive-fetch a `www.w3.org` URL (WCAG spec, CSS spec, WAI wiki) for sha256 pinning | it 403s behind a Cloudflare JS challenge regardless of User-Agent -- use the WebFetch tool for the actual quotes (it reads w3.org fine) and spend one of the 6 source slots on a curl-able corroborating secondary source (MDN, WebAIM, an official GitHub mirror) to get a real pinned hash; state the w3.org access gap explicitly in the brief rather than fabricating a hash for it -- arXMCP precedent: ui-uplift-m6 hit this for both https://www.w3.org/TR/WCAG22/ and https://www.w3.org/WAI/GL/wiki/Relative_luminance
regex-anchor-broadening-breaks-comment-safety | a fix proposes widening a `.match()`-anchored (position-0) regex line-scanner to a `.search()`-based "find keyword anywhere in the line" check, in a file where every existing regex in the same scanner is `.match()`-anchored | live-test the broadened pattern against a `--`/prose comment line containing the target keyword before recommending it -- anchoring at line-start is what keeps comments invisible to the scanner today (emergent property, not an explicit strip-comments step), so a naive `.search()` broadening silently starts flagging comments -- arXMCP precedent: server/handlers/lean_verify.py's `_declaration_names` (six regexes, all `.match()`) would newly flag `-- prove this theorem using induction` as an unresolved declaration site under a naive `re.search(r'\btheorem\b')`-style broadening (issue #382 fix), forcing complete=False on snippets handled correctly today; a narrower `\bin\b` + optional-modifiers + keyword match (mirroring Lean's own `<modifier> in <command>` grammar) catches the real set_option/open bug shapes without the false positive.
gh-code-search-phrase-spans-newlines | using `gh api search/code` or `gh search code` with a multi-word quoted phrase to test whether two tokens appear on the SAME physical source line (e.g. "prefix in KEYWORD" adjacency for a Lean/any-language grammar-frequency census) | GitHub's legacy code-search phrase match does NOT require the quoted phrase to be contiguous within one physical line -- a query for `"in theorem"` scoped to leanprover-community/mathlib4 (132 total_count) matched real source where `in` ends one line and `theorem` begins the next (`set_option maxHeartbeats 7000 in\ntheorem euclid ...`), not just genuine same-line hits. `total_count` alone cannot distinguish same-line from split-line occurrences, and it also silently includes unrelated-prose false positives (docstrings containing "in theorem names", "in Theorem 1.39", etc.) -- always re-fetch with `-H "Accept: application/vnd.github.text-match+json" --jq '.items[].text_matches[0].fragment'` and eyeball each fragment before reporting a frequency claim; used this to correctly find 0/26 same-line hits for `set_option maxHeartbeats ... in <decl>` in mathlib4 (2026-08-04) despite a nonzero co-occurrence total_count.

upstream-artifact-recommends-what-the-repo-rule-forbids | A discovery/planning artifact that the milestone descends from names a specific technique in its recommendation table, and the SAME artifact elsewhere records the Baseline/policy status that disqualifies it -- so an implementer reading only the recommendation ships a banned feature. Detection: for every technique the upstream artifact attaches to THIS milestone id, re-check it against the repo's own hard rules independently rather than trusting the artifact's own approval column; grep the artifact for the milestone tag (e.g. `grep -n "UPL-3" final-report.md`) and read EVERY row, not just the milestone's own section. Observed twice in arXMCP's 2026q3 uplift: `light-dark()` (refused by ui-uplift-m6, refusal now recorded at app.css:29-32) and `text-wrap: balance`, which final-report.md:492 attaches to UPL-3/ui-uplift-m7 while its own column reads "Newly Available -> Widely 2026-11-13" -- after that milestone's 2026-08-22 target end. Mitigation: verify status from a primary machine-readable source (api.webstatus.dev/v1/features/<id>) rather than the artifact's prose, and require the implementer to record the REFUSAL in a code comment so the next agent does not re-propose it -- the m6 precedent at app.css:29-32 is the template.

roadmap-item-drops-a-load-bearing-authored-value | The roadmap.yaml summary paraphrases an upstream design artifact and silently loses a specific authored value, while the acceptance criteria constrain that value only NEGATIVELY -- so an implementer can invent a replacement, pass every AC, and still discard the art direction. Detection: for each concrete value the summary mentions abstractly ("title on a clamp", "a spacing ramp"), grep the discovery/synthesis/final-report tree for the literal token (`clamp(`, `letter-spacing`) and diff the artifact's value set against the roadmap's. In ui-uplift-m7 every value survived (11/13/16/20, +0.06em) EXCEPT the clamp's three terms; the authored `clamp(1.5rem, 4vw + .5rem, 2.25rem)` exists only at art-direction-scout-brief.md:493 / synthesis.md:270 / final-report.md:193, and AC#3 says only "scales via clamp() rather than staying fixed at 32px". Mitigation: quote the artifact's literal value in the research brief with its file:line so Phase 2 cannot invent one, and check the recovered value against any range the design canon states -- here the recovered clamp min (24px) is 4px BELOW the canon's own 28-40 title floor, a conflict neither document flags.
