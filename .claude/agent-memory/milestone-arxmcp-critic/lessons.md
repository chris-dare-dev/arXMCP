2026-07-12 — data-plane-governance-m3 (docs-only trust/evidence policy): re-verify EVERY file:line the policy cites against live source — found `Spec.lean:52` pointed at the theorem type sig, not the `sorry` (actual :57). Cross-repo Lean citations (bstab/, stability-mflds/) drift and are unpinned; prefer citing by declaration name. find_equation retrieval_mode count (5) only holds when you count the index-set `ted_fused_eq` in equations.py, not just the handler literals. Reproduce a ledger exhaustiveness grep yourself before trusting "exactly N absence claims".
2026-07-12 — source-truth-m1 (per-revision documents registry + OAI-PMH license client): security axis was clean-by-design — verify the advisory license MIRROR against the SHIPPED policy, not just the milestone's own charter. The real find was cross-tier: `decide_license_status` maps arXiv-default `nonexclusive-distrib` → `not-allowlisted-open` and the coverage report glosses that bucket as "truncate at m4", but `ingest/store.py:321` stamps `license="arxiv-license"` on every arXiv chunk and `arxiv-license` ∈ `server/license_policy.py::OA_ALLOWLIST` → served FULL body. A milestone can treat an OA decision as "the owner's, pending" when it already shipped in an earlier tier (textbook-ingest-m11). Also: when a self-contained fetch RE-MIRRORS `ingest/oai_delta._fetch_page`, diff the mirror line-by-line — oai_license pins the redirect against a module CONSTANT while exposing a configurable `endpoint` param (oai_delta pins against the param), a latent dead-knob divergence. Confirm new tests actually PASS (ran all 4 files, 57 green), don't just read them.
2026-07-13 — source-truth-m2 (chunks schema v2 backfill): the 0-re-embed smoke ("0 embedding mismatches on 15,106 rows") does NOT prove the deliverable — embeddings are preserved on a chunk-id MISS too, so it says nothing about the source_span resolved-rate. The real HIT-path dependency is preamble reproduction (`_resolve_preamble_doc`→`extract_preamble` must return the same `preamble_text` as ingest-time, since `_compute_chunk_id` hashes `preamble_text + NFC(body_text)`), and EVERY backfill test stubs preamble to `lambda:None` on both build AND backfill — chunk_ids match by construction, real `extract_preamble` never exercised. Verify a backfill mirror function (`_rechunk_paper` vs `_chunk_paper_impl`) container-for-container AND check its parsed-dir constant equals the chunker's (`CORPUS_PARSED_DIR`==`PARSED_DIR` here — matched). Idempotency skip-gates that key on ONE resolved column (source_revision_id) can permanently freeze a DIFFERENT column's abstention (source_span) with no --force. A "safe-direction/airtight" truncation-fallback claim ignores that BPE re-tokenization at a boundary slice isn't monotonic.
2026-08-06 — desktop-distribution-spike-1: packaging evidence must use `lstat` and count symlinks separately; `Path.is_file()` plus `stat()` follows links and can overstate bundle size, while name/mode/size-only manifests miss equal-size mutations.
