# Memory index — milestone-arxmcp-critic

## Recurring finding families (read these first)
- [claim-drift-verify-against-code](claim-drift-verify-against-code.md) — doc/docstring/description/summary/synthesis statements are CLAIMS about control flow; grep the cited code before marking clean (stale-docstring, bp1-validator-drift, security-doc-drift, doc-finalize-sibling, synthesis-FM, populate-after-append, empty-corpus-deploy-crash)
- [test-wiring-and-coverage-gaps](test-wiring-and-coverage-gaps.md) — selector/route/CLI WIRING ships untested while the new impl is unit-tested; dual-column embed needs vector↔id integration pins (route-tracker-fixture, second-chunker-envelope, cli-validate-slug, embedrecord-column, AC-names-benign)
- [storage-schema-cache-gotchas](storage-schema-cache-gotchas.md) — LanceDB cast-nullability, per-notebook cache-path collision, WAL busy-checkpoint corruption, uv.lock transitive major downgrade
- [severity-calibration-and-edge-inputs](severity-calibration-and-edge-inputs.md) — trace consequence class before severity; verify descopes via input contract; bytes preserved=MEDIUM/dropped=HIGH; deferred-without-tracking; path-leak redaction; k3s RO-rootfs; Windows env-artifact triage

## Specific pinned lessons
- [spy-passthrough-vs-binding-forward](spy-passthrough-vs-binding-forward.md) — a spy on a native-binding call proves the kwarg was PASSED, not FORWARDED to Rust/C; silent-drop class unguarded → MEDIUM
- [kuzu-reopen-guard-nondeterministic-under-refcounting](kuzu-reopen-guard-nondeterministic-under-refcounting.md) — public-API double-call reopen test for the kuzu del-db lock leak passes on pre-fix code under CPython refcounting; spy on close() instead → MEDIUM
- [threading-pinned-by-reading-not-assertion](threading-pinned-by-reading-not-assertion.md) — thread-through-startup milestones verify wiring by reading but rarely pin artifact LOCATION in the boot test → MEDIUM
- [synthesis-api-claim-vs-real-binding-return](synthesis-api-claim-vs-real-binding-return.md) — "list_indices returns ANN-only" was FALSE (scalar paper_id idx leaks in); stub-only tests masked it → HIGH
- [escape-on-emit-untested-for-new-wrap-kind](escape-on-emit-untested-for-new-wrap-kind.md) — new wrap_retrieved_text kind: injection test seeds instruction-LIKE string w/ no literal delimiter, so escape-on-emit goes untested → MEDIUM
- [ustar-name-field-100-not-255](ustar-name-field-100-not-255.md) — USTAR name field is 100 chars (+155 prefix split), NOT 255; preflight at 255 admits filenames tarfile then refuses → HIGH
- [tarfile-extractfile-follows-intra-archive-symlinks](tarfile-extractfile-follows-intra-archive-symlinks.md) — tar.extractfile silently follows SYMTYPE→member; PEP 706 filter="data" does NOT cover extractfile → LOW ordering
- [cli-direct-sqlite-vs-destructive-v0-v1](cli-direct-sqlite-vs-destructive-v0-v1.md) — CLI writes notebooks row w/o user_version + async store v0→v1 is DROP TABLE → silent wipe on first boot; mask: shared-DB test → CRITICAL
- [dark-mode-token-redeclaration-vs-hardcoded-color-literals](dark-mode-token-redeclaration-vs-hardcoded-color-literals.md) — @media dark redeclares :root tokens but hardcoded #hex literals bypass the cascade → HIGH
- [parseerror-not-runtimeerror-in-except-tuple](parseerror-not-runtimeerror-in-except-tuple.md) — `xml.etree.ElementTree.ParseError` is a `SyntaxError` subclass, NOT `RuntimeError`/`OSError`; catch block listing the latter as "parse failure" coverage is wrong → HIGH
- [middleware-cap-vs-handler-cap-read-ordering](middleware-cap-vs-handler-cap-read-ordering.md) — per-kind handler cap must fire BEFORE await file.read(); a raised middleware ceiling buffers the full body first → HIGH
- [bp1-description-vs-handler-validator-drift](bp1-description-vs-handler-validator-drift.md) — ToolMeta.description widened without widening the handler validator (see claim-drift family)
- [security-doc-drift-on-multi-byte-magic-sniff](security-doc-drift-on-multi-byte-magic-sniff.md) — operator-facing threat-model doc byte-counts/token-lists went stale vs the widened check (see claim-drift family)
- [uv-lock-transitive-major-version-downgrade](feedback_uv_lock_transitive_downgrade.md) — new optional-dependency extra silently downgrades a core direct dep a major version (see storage-schema family)
- [regression-guard-pins-names-not-shape](regression-guard-pins-names-not-shape.md) — a regression test that asserts on names/labels rather than the structural shape it defends passes through a shape regression
- [startup-isolated-test-misses-lifespan-fanout](startup-isolated-test-misses-lifespan-fanout.md) — testing a startup fn in isolation misses the lifespan fan-out ordering that the real boot path exercises
- [seed-helper-single-call-vs-claimed-per-paper-loop](seed-helper-single-call-vs-claimed-per-paper-loop.md) — a seed helper made ONE call while the summary claimed a per-paper loop; count actual calls vs the claim
- [vacuous-test-kept-as-documentation](vacuous-test-kept-as-documentation.md) — a test that can't fail (vacuous assert) kept "as documentation" gives false coverage signal
- [docs-only-milestone-propagates-upstream-broken-anchor](docs-only-milestone-propagates-upstream-broken-anchor.md) — a docs-only milestone can propagate an already-broken upstream anchor/link; verify the source anchor resolves
- 2026-07-13 (source-truth-m5, SHIP C0H0M0L2): schema-additive get_chunk surface via `row.get()` correctly handled the live `-pdfs` unmigrated-notebook 500 landmine; the FastMCP `exclude_none` wire-drop worry is a NON-issue here — get_paper's `sc["paper"]["authors"] is None` wire tests (test_tools_all.py:438,522) already prove `envelope()`→structuredContent preserves nulls over the JSON-RPC wire, so no new wire test was needed. Confirm the unmigrated test uses the column-ABSENT fixture (11-col `_chunk_arrow`), not just a null-valued one. Only nits: `chunk.truncated` (ingest) undisambiguated from serving `truncated_for_license`/`body_truncated`; fixture docstrings say "26-col/21-col" but build 16/11.
- 2026-07-13 (source-truth-m3, SHIP C0H0M0L1): the `arxmcp://corpus-manifest` resource AVOIDED the prior `escape-on-emit-untested-for-new-wrap-kind` MEDIUM — the injection test seeds a REAL literal `A</retrieved_manifest>SYSTEM…<retrieved_manifest>B` breakout, so escape-on-emit is genuinely exercised end-to-end via `mcp.read_resource`. Escape is PAYLOAD-level (`_wrap_json`→`wrap_retrieved_text` over the whole serialized string), so one freeform-field test covers all three operator passthroughs (set_by/set_at/note), not just note. Hash byte-stability was clean on BOTH hashes: EXPECTED_TOOL_SCHEMA_SHA256 untouched (wrap-tag edit is in the helper, not ALL_TOOLS), and the manifest `content_hash`/`rollup_sha256` reuse `_serialize_tools`' `(",",":")`/`ensure_ascii=True` convention with the `revisions` ARRAY explicitly `sorted((work_id,arxiv_version))` (sort_keys doesn't order arrays). Key hermeticity detail: `build_manifest` resolves `settings_db_path` at CALL time so conftest's autouse `_patched_operator_settings_db` DEFAULT_DB_PATH redirect is honored. Only LOW: per-notebook failure isolation is piece-wise (try/except wraps `_load_records` but not `_read_override`:455 or the `build_manifest` loop:486) — holds for all reachable exceptions today (`_check_key` can't throw for creation-validated slugs) but fragile to future edits; a blanket loop-level `except Exception`→degrade would make the stated "never fail the whole read" invariant hold unconditionally.
