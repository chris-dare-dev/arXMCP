"""Observability subpackage (E14_S01).

Contains the request-level + embedder + reranker Prometheus
metric families. Cache + retrieval-cap + drift + eval metrics
remain in :mod:`server.metrics` for backwards-compat with
existing tests.
"""
